{
  "name": "Booking",
  "type": "object",
  "properties": {
    "lensman_id": {
      "type": "string",
      "description": "ID of the booked lensman"
    },
    "lensman_name": {
      "type": "string",
      "description": "Name of the lensman"
    },
    "lensman_email": {
      "type": "string",
      "description": "Email of the booked creator, used to scope access to the booking"
    },
    "client_id": {
      "type": "string",
      "description": "Client user ID when available"
    },
    "creator_id": {
      "type": "string",
      "description": "Creator/lensman ID for contract records"
    },
    "contract_id": {
      "type": "string",
      "description": "Generated booking contract ID"
    },
    "terms_version": {
      "type": "string",
      "description": "Terms version active when booking was made"
    },
    "privacy_version": {
      "type": "string",
      "description": "Privacy version active when booking was made"
    },
    "client_contract_accepted": {
      "type": "boolean",
      "default": false,
      "description": "Whether client accepted the booking contract"
    },
    "creator_contract_accepted": {
      "type": "boolean",
      "default": false,
      "description": "Whether creator accepted the booking contract"
    },
    "client_contract_accepted_at": {
      "type": "string",
      "format": "date-time",
      "description": "Client booking contract acceptance time"
    },
    "creator_contract_accepted_at": {
      "type": "string",
      "format": "date-time",
      "description": "Creator booking contract acceptance time"
    },
    "client_name": {
      "type": "string",
      "description": "Client full name"
    },
    "client_email": {
      "type": "string",
      "description": "Client email"
    },
    "client_phone": {
      "type": "string",
      "description": "Client phone"
    },
    "event_date": {
      "type": "string",
      "format": "date",
      "description": "Event date"
    },
    "event_time": {
      "type": "string",
      "description": "Event start time"
    },
    "event_type": {
      "type": "string",
      "enum": [
        "birthday",
        "dinner",
        "rooftop",
        "music_video",
        "restaurant_launch",
        "content_day",
        "proposal",
        "wedding",
        "corporate",
        "custom"
      ],
      "description": "Type of event"
    },
    "event_description": {
      "type": "string",
      "description": "Brief describing what the client wants captured"
    },
    "location": {
      "type": "string",
      "description": "Event location/address"
    },
    "attendees": {
      "type": "number",
      "description": "Number of attendees"
    },
    "package_type": {
      "type": "string",
      "enum": [
        "half_day",
        "full_day",
        "custom"
      ],
      "description": "Requested format"
    },
    "add_ons": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Add-ons requested by the client"
    },
    "total_price": {
      "type": "number",
      "description": "Agreed amount, set from the accepted quote"
    },
    "co_bookers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Emails invited to chip in toward the booking"
    },
    "album_access_emails": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Emails invited to access the final album without contributing"
    },
    "status": {
      "type": "string",
      "enum": [
        "requested",
        "quoted",
        "quote_accepted",
        "pending",
        "awaiting_creator_acceptance",
        "confirmed",
        "in_progress",
        "awaiting_delivery",
        "delivered",
        "completed",
        "cancelled",
        "disputed"
      ],
      "default": "requested",
      "description": "Booking status"
    },
    "payment_status": {
      "type": "string",
      "enum": [
        "pending",
        "held",
        "released",
        "refunded"
      ],
      "default": "pending",
      "description": "Payment/escrow status"
    },
    "delivery_deadline": {
      "type": "string",
      "format": "date",
      "description": "Expected delivery date"
    },
    "delivery_link": {
      "type": "string",
      "description": "URL to the delivered gallery, pasted by the creator"
    },
    "delivered_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the creator marked the gallery delivered"
    },
    "delivery_notes": {
      "type": "string",
      "description": "Notes from lensman on delivery"
    },
    "client_confirmed_delivery": {
      "type": "boolean",
      "description": "Whether client confirmed receipt"
    },
    "billing_address": {
      "type": "string",
      "description": "Client billing address"
    }
  },
  "required": [
    "lensman_id",
    "client_name",
    "client_email",
    "event_date",
    "event_type"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.client_id": "{{user.id}}"
        },
        {
          "data.client_email": "{{user.email}}"
        },
        {
          "data.lensman_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "$or": [
        {
          "data.client_id": "{{user.id}}"
        },
        {
          "data.client_email": "{{user.email}}"
        },
        {
          "data.lensman_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}